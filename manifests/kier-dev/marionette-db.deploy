#!/usr/bin/env bash
set -o errexit -o nounset -o pipefail

export release_name=marionette-db
export release_namespace=kier-dev
export chart_repo=https://charts.bitnami.com/bitnami
export chart_name=postgresql
export values=$(dirname $0)/marionette-db.values

exec $(dirname $0)/../../tools/helm-deploy.sh "$@"
